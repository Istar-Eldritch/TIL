
# Find what's taking space in your disk with du

I found myself lately wondering what was taking space in my hd. `du` is the perfect tools for this. It displays disk usage information for each file and directory in the folder specified.

```bash
du -sh ~
```
---

#### Tags
`space` `hd` `file` `size` `folder` `directory` `du`
